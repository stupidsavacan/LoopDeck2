# Copy this file to android/keystore.properties for local signed release builds.
# Do not commit android/keystore.properties or the JKS file.

storeFile=loopdeck-release.jks
storePassword=replace-with-keystore-password
keyAlias=replace-with-key-alias
keyPassword=replace-with-key-password
